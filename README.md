# Agent Second Brain

**Голосовой личный ассистент в Telegram с интеграцией Obsidian и Todoist**

Записываешь голосовые → бот транскрибирует → сохраняет в Obsidian → создаёт задачи в Todoist → присылает отчёты.

---

## Что умеет бот

| Функция | Описание |
|---------|----------|
| 🎤 Голосовые | Транскрибирует через Deepgram (русский язык) |
| 💬 Текст | Сохраняет как есть |
| 📷 Фото | Сохраняет с подписью |
| ⚙️ Обработка | Классифицирует записи, создаёт задачи, сохраняет мысли |
| 📅 Недельный дайджест | Анализирует неделю, показывает прогресс по целям |
| ✨ Произвольные запросы | "Перенеси задачи", "Покажи на сегодня" |

### Интерфейс

```
┌──────────────────────────────────────┐
│  🎤 [Голосовое сообщение]            │
│                                      │
│  ✓ Сохранено: 14:30 [voice]         │
│  "Надо обсудить проект с командой"  │
├──────────────────────────────────────┤
│ [📊 Статус] [⚙️ Обработать] [📅 Неделя] │
│ [✨ Запрос]        [❓ Помощь]       │
└──────────────────────────────────────┘
```

---

## Требования

- Компьютер с VS Code и Git
- VPS с Ubuntu 22.04+ (минимум 1GB RAM)
- Аккаунты: Telegram, Deepgram (бесплатно $200), Anthropic (Claude)
- Опционально: Todoist

---

# Часть 1: Подготовка на компьютере

Сначала настраиваем проект локально, потом переносим на сервер.

## Шаг 1: Установка Git и VS Code

### Windows

1. Скачайте [Git для Windows](https://git-scm.com/download/win)
2. Установите, оставив все настройки по умолчанию
3. Скачайте [VS Code](https://code.visualstudio.com/)
4. Установите

### Mac

1. Откройте Terminal (Cmd+Пробел → "Terminal")
2. Введите `git --version` — если не установлен, система предложит установить
3. Скачайте [VS Code](https://code.visualstudio.com/)

### Linux

```bash
sudo apt install git
# VS Code: https://code.visualstudio.com/
```

---

## Шаг 2: Клонирование проекта

1. Откройте VS Code
2. Нажмите `Ctrl+Shift+P` (Windows/Linux) или `Cmd+Shift+P` (Mac)
3. Введите "Git: Clone"
4. Вставьте URL: `https://github.com/smixs/dbrain.git`
5. Выберите папку, куда сохранить (например, Documents)
6. Нажмите "Open" когда спросит

Теперь у вас открыт проект в VS Code.

---

## Шаг 3: Очистка чужих данных

В левой панели VS Code найдите и **удалите** содержимое этих папок:

```
vault/daily/           ← удалить все файлы внутри
vault/thoughts/ideas/  ← удалить все файлы
vault/thoughts/projects/
vault/thoughts/learnings/
vault/thoughts/reflections/
vault/summaries/       ← удалить все файлы
vault/attachments/     ← удалить все папки внутри
```

**Как удалить:** правый клик на файл → Delete

---

## Шаг 4: Заполнение целей (с AI-помощником)

Это самый важный шаг! Бот будет использовать ваши цели для приоритизации задач.

### Вариант A: С помощью ChatGPT/Claude

1. Откройте ChatGPT или Claude в браузере
2. Скопируйте туда содержимое файла `vault/goals/0-vision-3y.md`
3. Напишите:

```
Ты мой личный коуч. Помоги мне заполнить этот шаблон 3-летнего видения.
Задавай мне вопросы по одному, чтобы понять:
- Где я хочу быть через 3 года в карьере?
- Какое здоровье и энергия мне нужны?
- Какие отношения я хочу?
- Что для меня финансовая свобода?
- Что приносит мне радость?

После каждого моего ответа задавай уточняющие вопросы.
В конце сформируй заполненный шаблон.
```

4. Ответьте на вопросы AI
5. Скопируйте результат обратно в файл

Повторите для:
- `vault/goals/1-yearly-2026.md` — годовые цели
- `vault/goals/2-monthly.md` — приоритеты на месяц
- `vault/goals/3-weekly.md` — фокус на неделю

### Вариант B: Самостоятельно

Откройте каждый файл в VS Code и заполните своими целями.

**Важно:** В файле `3-weekly.md` есть секция "ONE Big Thing" — это ваш главный фокус на неделю. Бот показывает его в каждом отчёте.

---

## Шаг 5: Настройка профиля

Откройте файл:
```
vault/.claude/skills/dbrain-processor/references/about.md
```

Замените информацию на свою:
- Имя
- Чем занимаетесь
- Основные проекты
- Приоритеты

Это поможет боту понимать контекст ваших записей.

---

## Шаг 6: Создание своего репозитория

1. Зайдите на [GitHub](https://github.com)
2. Войдите или создайте аккаунт
3. Нажмите "+" → "New repository"
4. Имя: `my-second-brain` (или любое)
5. **Важно:** Оставьте все галочки пустыми (без README, без .gitignore)
6. Нажмите "Create repository"
7. Скопируйте URL вашего репозитория (например: `https://github.com/YOUR_NAME/my-second-brain.git`)

---

## Шаг 7: Публикация в свой репозиторий

В VS Code:

1. Откройте Terminal: меню View → Terminal (или `Ctrl+``)
2. Введите команды:

```bash
git remote set-url origin https://github.com/YOUR_NAME/my-second-brain.git
```

3. В левой панели нажмите на иконку с веткой (Source Control)
4. Вы увидите список изменённых файлов
5. Нажмите "+" рядом с "Changes" чтобы добавить все файлы
6. В поле "Message" напишите: `Initial setup with my goals`
7. Нажмите кнопку "Commit"
8. Нажмите "Sync Changes" или "Publish Branch"

Если спросит логин — введите ваш GitHub логин и пароль (или Personal Access Token).

**Готово!** Ваш проект с вашими целями теперь в вашем репозитории.

---

# Часть 2: Получение токенов

Перед настройкой сервера получите все нужные токены.

## Telegram Bot Token

1. Откройте Telegram
2. Найдите `@BotFather`
3. Отправьте `/newbot`
4. Введите имя бота: `My Second Brain`
5. Введите username: `my_brain_bot` (должен заканчиваться на `bot`)
6. Скопируйте токен:
   ```
   7123456789:AAHdN8J2K4m5N6o7P8q9R0s1T2u3V4w5X6y
   ```

**Сохраните токен!**

## Ваш Telegram ID

1. Найдите `@userinfobot` в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID: `123456789`

**Сохраните ID!**

## Deepgram API Key

1. Зайдите на [console.deepgram.com](https://console.deepgram.com/)
2. Зарегистрируйтесь (бесплатно, $200 на старте)
3. Settings → API Keys → Create API Key
4. Скопируйте ключ (показывается один раз!)

**Сохраните ключ!**

## Todoist API Token (опционально)

1. [todoist.com](https://todoist.com) → Settings → Integrations → Developer
2. Скопируйте API token

---

# Часть 3: Настройка VPS

Теперь переносим проект на сервер.

## Шаг 1: Покупка VPS

Рекомендую [Contabo](https://contabo.com) — дёшево и надёжно.

- Выберите Cloud VPS
- ОС: Ubuntu 22.04
- Минимум: 1GB RAM (лучше 2GB)
- Получите: IP адрес и пароль root

## Шаг 2: Подключение к серверу

**Windows:** Скачайте [PuTTY](https://www.putty.org/) или используйте Windows Terminal

**Mac/Linux:** Откройте Terminal

```bash
ssh root@ВАШ_IP_АДРЕС
```

Введите пароль (символы не отображаются — это нормально).

## Шаг 3: Создание пользователя

```bash
# Создаём пользователя
adduser myuser

# Даём права sudo
usermod -aG sudo myuser

# Переключаемся
su - myuser
```

## Шаг 4: Установка всего необходимого

Копируйте и вставляйте эти команды по одной:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Базовые инструменты
sudo apt install -y git curl wget build-essential

# Python 3.12
sudo apt install -y software-properties-common
sudo add-apt-repository -y ppa:deadsnakes/ppa
sudo apt update
sudo apt install -y python3.12 python3.12-venv python3.12-dev

# uv (менеджер пакетов Python)
curl -LsSf https://astral.sh/uv/install.sh | sh
source ~/.bashrc

# Node.js 20
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Claude CLI
npm install -g @anthropic-ai/claude-code

# Авторизация Claude (следуйте инструкциям)
claude auth login
```

## Шаг 5: Клонирование ВАШЕГО репозитория

```bash
cd ~
mkdir -p projects
cd projects

# Замените URL на ваш репозиторий!
git clone https://github.com/YOUR_NAME/my-second-brain.git dbrain
cd dbrain

# Установка зависимостей
uv sync
```

## Шаг 6: Настройка .env

```bash
nano .env
```

Вставьте (замените на ваши токены):

```bash
TELEGRAM_BOT_TOKEN=7123456789:AAHxxxxxxxxxxxxxxxxxxxxxxxxxx
DEEPGRAM_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TODOIST_API_KEY=
VAULT_PATH=./vault
ALLOWED_USER_IDS=[123456789]
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

## Шаг 7: Тестовый запуск

```bash
uv run python -m d_brain
```

Откройте Telegram, найдите вашего бота, отправьте `/start`.

Если работает — `Ctrl+C` для остановки.

## Шаг 8: Автозапуск

```bash
sudo nano /etc/systemd/system/d-brain-bot.service
```

Вставьте (замените `myuser`):

```ini
[Unit]
Description=d-brain Telegram Bot
After=network.target

[Service]
Type=simple
User=myuser
WorkingDirectory=/home/myuser/projects/dbrain
ExecStart=/home/myuser/.local/bin/uv run python -m d_brain
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable d-brain-bot
sudo systemctl start d-brain-bot

# Проверка
sudo systemctl status d-brain-bot
```

---

# Часть 4: Синхронизация с Obsidian

## На компьютере

Ваш репозиторий уже клонирован (из Части 1). Откройте папку `vault` как хранилище в Obsidian.

## Автоматическая синхронизация

Создайте скрипт для синхронизации каждый час:

**Mac:** Создайте файл `~/sync-brain.sh`:

```bash
#!/bin/bash
cd ~/Documents/my-second-brain  # путь к вашему репозиторию
git pull --rebase
git add -A
git commit -m "Auto-sync: $(date '+%Y-%m-%d %H:%M')" 2>/dev/null || true
git push 2>/dev/null || true
```

```bash
chmod +x ~/sync-brain.sh
```

Добавьте в cron:
```bash
crontab -e
# Добавьте строку:
0 * * * * ~/sync-brain.sh
```

**Windows:** Используйте Task Scheduler с аналогичным bat-файлом.

---

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу |
| `/status` | Сколько записей сегодня |
| `/process` | Обработать записи |
| `/weekly` | Недельный дайджест |
| `/do [текст]` | Произвольный запрос |

### Примеры /do

```
/do покажи задачи на сегодня
/do перенеси просроченные на понедельник
/do что я записывал вчера?
```

---

## Полезные команды VPS

```bash
# Логи
sudo journalctl -u d-brain-bot -f

# Перезапуск
sudo systemctl restart d-brain-bot

# Обновление
cd ~/projects/dbrain
git pull
uv sync
sudo systemctl restart d-brain-bot
```

---

## Решение проблем

**Бот не отвечает:**
```bash
sudo systemctl status d-brain-bot
sudo journalctl -u d-brain-bot -n 50
```

**Не транскрибирует:**
Проверьте DEEPGRAM_API_KEY в .env

**Ошибка Claude:**
```bash
claude auth status
# Если не авторизован:
claude auth login
```

---

## FAQ

**Можно без Todoist?** — Да, оставьте TODOIST_API_KEY пустым.

**Можно без Obsidian?** — Да, файлы можно смотреть на GitHub или в любом редакторе.

**Сколько стоит?**
- Deepgram: бесплатно $200
- Claude: ~$20/месяц при активном использовании
- VPS: от $5/месяц

---

*Made with 🧠 for people who think out loud*
